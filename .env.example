# Cloudflare account ID — wrangler reads this automatically when not set in wrangler.jsonc
CLOUDFLARE_ACCOUNT_ID=

# Turnstile bot protection
# Site key (public) is in wrangler.jsonc vars as TURNSTILE_SITE_KEY
# Secret (server-side) goes in .dev.vars for local dev, `wrangler secret put TURNSTILE_SECRET` for prod
# When TURNSTILE_SECRET is not set, Turnstile verification is skipped (safe for local dev)
